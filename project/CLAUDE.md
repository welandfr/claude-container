# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is a bare-bones static HTML project. The only file is `index.html` — a plain HTML5 skeleton with no framework, build step, or dependencies.

## Development

Open `index.html` directly in a browser. No build, install, or server step is required unless one is introduced later.
